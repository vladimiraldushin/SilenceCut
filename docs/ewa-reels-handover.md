# EWA Reels «Неопознанный объект» — состояние работы

Дедлайн сдачи: **12.08.2026, 21:00 МСК**.

Задание академии лидеров EWA: ролик 45–60 секунд про продукт NO SMOKE в жанре
псевдорепортажа. Собранный ролик длится 125,8 с — это вдвое дольше заявленного, вопрос
сокращения с заказчиком не поднимался.

## Где что лежит

| Что | Путь |
|---|---|
| Проект монтажа | `~/Downloads/EWA_Reporters_reels.silencecut` |
| Готовый файл | `~/Downloads/EWA_Reporters_reels_final.mp4` |
| Копия до вклейки графики | `~/Downloads/EWA_Reporters_reels.pre-graphics.silencecut` |
| Копия до правок звука | `~/Downloads/EWA_Reporters_reels.backup.silencecut` |
| Исходники съёмки | `~/Downloads/IMG_86xx.MOV` |
| Логотипы, фонограмма | `~/Downloads/EWA /` |
| Графика (Remotion) | `~/Projects/tessera-video-studio/studio/src/silencecut/` |
| Готовые файлы графики | `~/Projects/tessera-video-studio/output/graphics/` |

## Как собран ролик

| Отрезок | Что |
|---|---|
| 0 – 15,60 | заставка `ewa-opener.mp4` с фонограммой «Время» |
| 15,60 – 115,42 | репортаж |
| 115,42 – 125,82 | концовка `ewa-outro.mp4` с озвучкой |

- **Громкость выровнена по клипам.** 16 клипов были тише медианы на 9–17 дБ (микрофон
  пропадал на отдельных репликах). Подняты командой `level-audio --apply`. Два клипа
  подняты не полностью — упёрлись в пик.
- **Наезд на финале.** Клип 112,67–115,42 («Это ноу смоук») ведёт девушку от среднего
  плана к крупному: `framing` 1.5/−0.26/0.10 → `framingEnd` 2.1/−0.16/0.14.
- **Бегущая строка** `ticker-full.mov` — 15,60 → 115,40, то есть от конца заставки до
  начала концовки. Тексты — новости про другие продукты EWA в духе сюжета.
- **Четыре связки** между сценами. Они с альфой и лежат ПОВЕРХ стыка, а не вставлены в
  хребет: шторки сходятся ровно в момент склейки, поэтому времени не добавляют и тайминги
  подписей не двигают. Номера сюжетов убраны по просьбе заказчика.

| Связка | Стоит | Накрывает стык |
|---|---|---|
| ОЧЕВИДЦЫ | 39,42 | 39,82 |
| ВТОРОЕ ДЫХАНИЕ | 62,53 | 62,93 |
| ОГОРОД | 77,46 | 77,86 |
| ЭКСПЕРТИЗА | 106,64 | 107,04 |

Подписи говорящих (тайминги проверены заказчиком, сдвинуты на +15,60 вместе с заставкой):

| Время | Имя | Роль |
|---|---|---|
| 17,80–21,80 | Владимир Алдушин | Специальный репортёр EWA |
| 43,60–47,60 | Дубинчина Олеся | Курильщик со стажем |
| 54,00–58,00 | Фёдорова Вера | Начинающий курильщик |
| 64,00–66,90 | Пашаян Милана | Зожник |
| 67,10–70,60 | Матрёнина Евгения | Подруга зожницы |
| 82,60–85,80 | Виктория Алдушина | Садовник любитель |
| 90,70–93,70 | Галич Мария | Садовод нелюбитель |
| 107,80–111,30 | Ольга Канунникова | Доктор несуществующих наук |

Тайминги привязаны к ПЕРВОЙ реплике человека, а не к порядку в кадре. Заказчик правил их
дважды — подпись не должна появляться раньше, чем человек попал в кадр, и не должна
задерживаться после того, как заговорил следующий. Вырез или вставка на таймлайне двигает
подписи вместе с картинкой (`applyRemovals` / `applyInsertion`), поэтому руками их
поправлять не нужно — но ТОЛЬКО если правка идёт через эти методы.

## Незакрытое

Хронометраж 125,8 с против заявленных 45–60.

## Грабли, на которые уже наступили

- **Заставка ушла в монтаж немой.** В шаблоне `NewsOpener` не было `<Audio>`: вся
  хореография сделана под удары фонограммы, а самой фонограммы в рендере не было, и это
  заметил заказчик, а не я. Кадры я проверял, звук новых кусков — нет. Теперь музыка едет
  в самом шаблоне (`music` + `musicGain`), а готовый файл пересобран. Правило: у каждого
  нового куска мерить И картинку, И уровень — `ffmpeg -af volumedetect` по отрезку.
- **Установленный `silencecut` — отдельный бинарь, и он не обновляется сам.** Правка в
  `Sources/` попадает в него только после `scripts/install-cli.sh`. Час ушёл на поиск
  «ошибки AVFoundation», которой не было: CLI в PATH был собран на 50 минут раньше, чем
  появилась раскладка графики по нескольким дорожкам, и честно работал по старому коду —
  вторая надпись молча пропадала.
- **Запущенное приложение старой сборки затирает новые поля проекта.** Оно следит за
  файлом, подхватывает правку CLI и сохраняет обратно своим кодировщиком. Поля `gain` и
  `framingEnd` так исчезли дважды. Перед правкой проекта через CLI приложение надо либо
  закрыть, либо пересобрать.
- **`TimelineClip` имеет собственный `encode(to:)`** — добавленное поле нужно вписывать
  туда руками, иначе оно молча теряется при сохранении. Сторожит тест
  `clipSurvivesRoundTripWithEveryField`.
- **Проверять звук измерением, а не на слух.** Дважды казалось, что правка применилась,
  хотя уровни на выходе не менялись. Быстрый способ: экспорт + `ffmpeg -af volumedetect`
  по нужным отрезкам.
- **Судить о картинке по расшифровке речи нельзя.** Пролёт коробки NO SMOKE я записал в
  отсутствующие, потому что он беззвучный. Для визуальных вопросов — `frame --sheet`.

## Полезные команды

```bash
silencecut info ~/Downloads/EWA_Reporters_reels.silencecut
silencecut list <проект> --graphics          # что и когда стоит на слое графики
silencecut list <проект> --clips             # гейн и наличие наезда по клипам
silencecut frame <проект> --count 15 --sheet # посмотреть монтаж глазами
silencecut level-audio <проект>              # без --apply только показывает
silencecut transcribe <проект>               # расшифровка, в проект не пишет
silencecut export <проект> --out готовое.mp4
```
